# 变更记录

本文件记录实现行为。实现版本与模型文件格式分别管理；本记录不表示已发布 Git 标签或软件包。

## 结构 VAE 训练更新 — 2026-09-23

- 将结构 VAE 的默认隐藏层宽度从 64 调整为 128，并重新训练 DeepCAD 候选模型。
- 训练 CLI 新增可配置的早停耐心值和数量遮蔽概率；实验报告完整保存运行配置。
- 在相同 242/59 训练测试划分上，标签 Brier 误差从 0.062019 降至 0.006691，数量标准化误差从 0.165638 降至 0.111549，检索代理一致率从 93.22% 升至 94.92%。
- 独立重载审计覆盖 301 个样本并通过；SVD 仍保持更低的结构重建误差。

## 3.0.0 — 2026-09-20

### 新增

- 可复用的 `VaeRuntime`，支持批量编码、后验参数查询、解码与重建。
- `vae_cli.py` 数值向量批量接口。
- 几何描述与潜空间的混合距离检索。
- 小批量训练、KL 权重预热和可追溯的数据划分索引。
- 两组数据的基线比较脚本、实验记录及回归测试。

### 修复

- 从头训练时，归一化统计只使用训练子集，避免验证数据参与统计。
- 微调保留完整预训练权重及归一化统计，避免部分层重新初始化。
- 初始权重纳入验证目标最优候选，避免训练后无条件保留更差权重。

### 兼容性与限制

- 模型格式保持 `ai-cad-brep-vae-v2`；无几何索引的旧模型回退到潜空间检索。
- 文本结构线性基线保留独立接口和格式。
- 解码生成描述向量，不生成 STEP/BREP 实体。
- 微调重新初始化优化器；不支持精确断点续训。

## 文档整理 — 2026-09-20

- 重写项目首页，增加可执行的最小示例。
- 增加使用指南、参数与模型格式参考、评估方法和完整结果说明。
- 明确数据可用性、兼容性及实验结论的适用范围。

## 项目更名 — 2026-09-20

- 项目正式命名为 **FormaLatent（形潜）**，仓库名称改为 `formalatent`。
- 项目仍属于 AI-CAD；Python 接口与模型格式标识保持兼容。

## 结构神经 VAE 与外部样本训练 — 2026-09-21

- 新增结构神经 VAE、批量编码/解码接口；训练器默认使用神经 backend，`--backend svd` 保留旧基线。
- 标签使用 BCE 重建目标，数量使用标准化 log1p MSE，质量元数据只参与排序。
- 新增 AI-CAD 的 JS 后验均值推理与模型加载补丁，以及 Python/JS 数值一致性验证。
- 通过 AnySearch 确认 DeepCAD 官方来源，复用本机归档抽取 320 个候选样本；保留 242 个训练样本和 59 个独立测试样本。
- 训练两条神经 VAE，保存潜向量及重建输出，并提供文件哈希、来源及独立重载审计。结果保留了 SVD 更优的重建指标，未将功能验证等同于全面质量提升。
